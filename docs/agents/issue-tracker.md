# Issue tracker

Issues for this repo are plain markdown files under `docs/issues/`, named
`<number>-<slug>.md`.

To fetch the spec for an issue referenced in a commit message as `#12`, read
`docs/issues/12-*.md`. There is no external tracker and no API to call.
