STEP 1: dispatch.

Send one message containing one Agent tool call per lens listed below.
The lenses will run concurrently.
Set `run_in_background: false` on each call to receive the reports inline in the same turn.

__LENS_LIST__

Each lens's system prompt includes its review method and output requirements.
Use the following prompt for each lens:

__DISPATCH__
