{
    "name": "codeferret",
    "private": true,
    "description": "The action and the plugin run the scripts under review/ and scripts/ with bun directly, so there is no build step and no bundle that can fall out of date.",
    "scripts": {
        "typecheck": "tsc --noEmit",
        "test": "bun test",
        "prepare": "lefthook install"
    },
    "devDependencies": {
        "@types/bun": "^1.3.5",
        "lefthook": "^2.1.10",
        "typescript": "^5.9.3"
    }
}
